<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gr.farming.cash.model.CashDAO">	
	<insert id="insert" parameterType="cashVo">
		<selectKey keyProperty="cashNo" resultType="int" order="BEFORE">
			select cash_seq.nextval from dual
		</selectKey>
		insert into cash(cash_no, cash_pay, balance ,member_no) 
		values(#{cashNo}, #{cashPay}, #{balance}, #{memberNo})
	</insert>
	
	<select id="selectAll" resultType="cashVo">
		select * from cash 
		where member_no=#{memberNo}
		order by cash_no desc
	</select>
	
	
</mapper>