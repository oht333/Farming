<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gr.farming.cash.model.CashDAO">	
	<insert id="insert" parameterType="cashVo">
		<selectKey keyProperty="cashNo" resultType="int" order="BEFORE">
			select cash_seq.nextval from dual
		</selectKey>
		insert into cash(cash_no, merchant_uid, member_id, charge)
		values(#{cashNo}, #{merchantUid}, #{memberId}, #{charge})
	</insert>
	
	<update id="plusBal" parameterType="cashVo">
		update cash
		set balance = balance + ${charge}
		where member_id=#{memberId}
	</update>
	
	
</mapper>


