<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gr.farming.order.model.OrderDAO">	
	<!-- 주문등록 --> 
	<insert id="insert" parameterType="orderVo">
		insert into orders(merchant_uid, member_name, expert_name, service_no, service_name, price, state)
		values(#{merchantUid}, #{memberName}, #{expertName}, #{serviceNo}, #{serviceName}, #{price}, #{state})
	</insert>
	
	
</mapper>


